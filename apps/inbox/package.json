{
  "name": "@rafters-studio/inbox",
  "private": true,
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rafters/mail-cloudflare": "^0.1.0",
    "@rafters/mail-drizzle": "^0.1.0",
    "drizzle-orm": "^0.45.2",
    "uuidv7": "^1.1.0"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "^5.9.3",
    "wrangler": "^4.75.0"
  }
}
